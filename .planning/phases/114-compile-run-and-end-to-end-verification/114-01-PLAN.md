---
phase: 114-compile-run-and-end-to-end-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mesher/mesher
  - SERVICE_CALL_SEGFAULT.md
autonomous: false
requirements: [VER-01]

must_haves:
  truths:
    - "`/Users/sn0w/Documents/dev/snow/target/debug/meshc build mesher` exits with code 0 and produces the `mesher/mesher` binary"
    - "Mesher starts, connects to PostgreSQL at postgres://mesh:mesh@localhost:5432/mesher, and prints `[Mesher] Foundation ready`"
    - "`meshc migrate up` completes without error (schema is up to date)"
    - "The MirType::Tuple SIGSEGV fix is confirmed present in types.rs and the live binary does not crash on the first authenticated event POST"
  artifacts:
    - path: "mesher/mesher"
      provides: "Compiled Mesher binary with ORM query layer"
      min_lines: 0
  key_links:
    - from: "mesher/main.mpl"
      to: "mesher/mesher"
      via: "meshc build mesher"
      pattern: "meshc build"
    - from: "mesher/mesher"
      to: "postgres://mesh:mesh@localhost:5432/mesher"
      via: "Pool.open in main()"
      pattern: "Connecting to PostgreSQL"
---

<objective>
Verify zero-error compilation of Mesher with the fully rewritten ORM query layer, confirm successful startup with PostgreSQL, run migrations, and confirm the MirType::Tuple SIGSEGV fix is active.

Purpose: Phase 113 completed the ORM rewrite. Phase 114 must prove the full build pipeline works end-to-end before HTTP/WS endpoint testing begins.
Output: A freshly compiled `mesher/mesher` binary, confirmed startup log, and documented SIGSEGV resolution status.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-mesher-rewrite-retention-and-final-cleanup/113-01-SUMMARY.md

<interfaces>
<!-- Compiler invocation pattern (established in prior phases) -->
<!-- meshc binary is at /Users/sn0w/Documents/dev/snow/target/debug/meshc (not the installed ~/.local/bin/meshc which lacks libmesh_rt.a) -->
<!-- Build command: run from /Users/sn0w/Documents/dev/snow -->

Compile:   /Users/sn0w/Documents/dev/snow/target/debug/meshc build mesher
Migrate:   /Users/sn0w/Documents/dev/snow/target/debug/meshc migrate up mesher
Migration file: mesher/migrations/20260216120000_create_initial_schema.mpl
Binary output: mesher/mesher

<!-- SIGSEGV fix location (from SERVICE_CALL_SEGFAULT.md RESOLVED section) -->
File: crates/mesh-codegen/src/codegen/types.rs
Fix: MirType::Tuple(_) => context.ptr_type(...).into()   (heap-allocated ptr, not by-value struct)
Status: CONFIRMED PRESENT (verified in current codebase)

<!-- PostgreSQL connection (from mesher/main.mpl) -->
Pool.open("postgres://mesh:mesh@localhost:5432/mesher", 2, 10, 5000)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile Mesher from clean and confirm zero errors</name>
  <files>mesher/mesher</files>
  <action>
    From /Users/sn0w/Documents/dev/snow, run the local meshc build:

    ```
    /Users/sn0w/Documents/dev/snow/target/debug/meshc build mesher
    ```

    Expected: exits with code 0, produces `mesher/mesher` binary. If there are compilation errors, read the output carefully and fix them in the relevant .mpl source files. Common issues to watch for:
    - Type mismatches introduced during ORM rewrite (check queries.mpl callers)
    - Import errors if function signatures changed
    - Any remaining raw SQL calls that fail to compile

    Also verify the MirType::Tuple fix is present by reading `crates/mesh-codegen/src/codegen/types.rs` lines 43-52 and confirming the `MirType::Tuple(_)` arm returns `context.ptr_type(...)` not a struct type.
  </action>
  <verify>
    <automated>/Users/sn0w/Documents/dev/snow/target/debug/meshc build mesher 2>&1; echo "exit:$?"</automated>
  </verify>
  <done>Build exits with code 0. Binary at mesher/mesher is newer than the most recently modified .mpl file. No compilation errors in output. MirType::Tuple arm confirmed as ptr type in types.rs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run migrations and verify Mesher startup against PostgreSQL</name>
  <files>mesher/mesher</files>
  <action>
    Human verification required: start Mesher against a live PostgreSQL instance and confirm it reaches "Foundation ready" with no crash.

    Perform these steps with a running PostgreSQL instance (postgres://mesh:mesh@localhost:5432/mesher):

    1. Run migrations (ensure schema is up to date):
       ```
       /Users/sn0w/Documents/dev/snow/target/debug/meshc migrate up mesher
       ```
       Expected: Exits 0 with migration applied or "already up to date" message.

    2. Start Mesher in the background, capture output for 3 seconds:
       ```
       cd /Users/sn0w/Documents/dev/snow
       ./mesher/mesher &
       sleep 3
       kill %1
       ```
       Expected startup lines (order may vary):
       - `[Mesher] Connecting to PostgreSQL...`
       - `[Mesher] Partitions created (7 days ahead)` (or similar partition message)
       - `[Mesher] OrgService started`
       - `[Mesher] ProjectService started`
       - `[Mesher] UserService started`
       - `[Mesher] Foundation ready`
       - `[Mesher] WebSocket server starting on :8081`
       - `[Mesher] HTTP server starting on :8080`

    3. Confirm no crash/SIGSEGV during startup. The binary should reach "Foundation ready" and begin serving.

    If PostgreSQL is not running locally, start it first:
    ```
    brew services start postgresql@15  # or equivalent
    createuser -s mesh 2>/dev/null; createdb -O mesh mesher 2>/dev/null
    psql -U mesh -d mesher -c "ALTER USER mesh WITH PASSWORD 'mesh';"
    ```
  </action>
  <verify>
    - `meshc migrate up mesher` exits 0
    - Mesher startup log shows `[Mesher] Foundation ready`
    - No SIGSEGV or panic in startup log
    - Mesher process is still alive after 3 seconds (does not exit with code 139)
  </verify>
  <done>Migrations applied, Mesher started and reached "[Mesher] Foundation ready" with no crash. Type "approved" or describe any failure.</done>
</task>

</tasks>

<verification>
1. `meshc build mesher` exits 0 — VER-01 (compilation)
2. Mesher starts and prints "[Mesher] Foundation ready" — VER-01 runtime startup
3. `meshc migrate up mesher` exits 0 — migration layer functional
4. No SIGSEGV during startup — SIGSEGV fix confirmed active
</verification>

<success_criteria>
- mesher/mesher binary exists and is newer than source files
- Compilation output contains zero errors
- Startup log shows PostgreSQL connection + all services started + "Foundation ready"
- MirType::Tuple fix confirmed as ptr type in types.rs
- No crash or SIGSEGV observed during startup
</success_criteria>

<output>
After completion, create `.planning/phases/114-compile-run-and-end-to-end-verification/114-01-SUMMARY.md` using the standard summary template.
</output>
