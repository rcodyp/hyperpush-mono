---
phase: 114-compile-run-and-end-to-end-verification
plan: 02
type: execute
wave: 2
depends_on: [114-01]
files_modified: []
autonomous: false
requirements: [VER-02, VER-03]

must_haves:
  truths:
    - "At least one HTTP endpoint in each domain (org/project/issue CRUD, search, dashboard, alerts, retention/settings) returns a 2xx JSON response with expected data"
    - "The WebSocket endpoint at :8081 accepts a connection and completes the HTTP upgrade (101 Switching Protocols)"
    - "The EventProcessor service call path (POST /api/v1/events with valid API key) does not crash with SIGSEGV on the first request"
    - "All HTTP endpoints that previously used raw SQL now route through ORM query paths without runtime panics or unexpected 500 errors"
  artifacts:
    - path: "mesher/mesher"
      provides: "Running Mesher process serving HTTP on :8080 and WS on :8081"
      min_lines: 0
  key_links:
    - from: "curl POST /api/v1/events"
      to: "EventProcessor.process_event service call"
      via: "ingestion/routes.mpl handle_event_authed -> RateLimiter.check_limit -> StorageWriter -> EventProcessor"
      pattern: "200 OK or 202 Accepted"
    - from: "WebSocket client"
      to: "mesher/ingestion/ws_handler.mpl ws_on_connect"
      via: "Ws.serve on port 8081"
      pattern: "HTTP/1.1 101 Switching Protocols"
---

<objective>
Perform smoke-test verification of all HTTP API endpoint domains and the WebSocket endpoint against a running Mesher instance, confirming the ORM rewrite produces correct responses and the EventProcessor service call SIGSEGV is not present.

Purpose: After compilation and startup are verified (Plan 01), this plan exercises all the rewritten query paths end-to-end via real HTTP requests.
Output: Documented HTTP response codes and payloads for each domain, WebSocket upgrade confirmation, SIGSEGV status resolved.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-compile-run-and-end-to-end-verification/114-01-SUMMARY.md

<interfaces>
<!-- HTTP routes (from mesher/main.mpl) -->
Base URL: http://localhost:8080
WS URL:   ws://localhost:8081

POST /api/v1/events                              -- event ingestion (requires X-Api-Key header)
GET  /api/v1/projects/:project_id/issues         -- search issues
GET  /api/v1/projects/:project_id/dashboard/volume -- dashboard volume
GET  /api/v1/projects/:project_id/alert-rules    -- list alert rules
GET  /api/v1/projects/:project_id/settings       -- project settings
GET  /api/v1/projects/:project_id/storage        -- project storage

<!-- Auth setup (from mesher/storage/queries.mpl auth domain) -->
To test authenticated endpoints, must first:
1. Create org: no auth endpoint available -- use psql or the user/org service directly
2. OR: use an existing test org/project/api-key from a prior test run

<!-- Test data setup approach -->
Since Mesher has no unauthenticated org creation endpoint in the HTTP router,
use psql to seed test data, then test with the seeded API key.

Seed SQL pattern (from queries.mpl create_user/create_org/create_project/create_api_key):
  INSERT INTO users (name, email, password_hash) VALUES ('test', 'test@test.com', crypt('pass', gen_salt('bf')));
  INSERT INTO orgs (name, owner_id) VALUES ('testorg', 1);
  INSERT INTO projects (name, org_id) VALUES ('testproject', 1);
  INSERT INTO api_keys (key_hash, project_id, name) VALUES (encode(gen_random_bytes(24),'hex'), 1, 'test');
  -- Note: retrieve the unhashed key via: SELECT key_hash FROM api_keys WHERE project_id = 1;
  --   (In the actual app, key_hash stores the raw key and authenticate uses crypt comparison)

<!-- SERVICE_CALL_SEGFAULT.md: RESOLVED section confirms -->
Fix: MirType::Tuple -> ptr (types.rs). All 509 runtime tests pass. SIGSEGV no longer occurs.
The live Mesher retest (rebuild from clean, test first authenticated event) is the remaining
verification step documented in SERVICE_CALL_SEGFAULT.md "Remaining items" section.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: HTTP API endpoint smoke test and WebSocket upgrade verification</name>
  <files>SERVICE_CALL_SEGFAULT.md</files>
  <action>
    Human verification required. Start Mesher if not already running, seed test data, then test all HTTP API domains and WebSocket endpoint.

    **Start Mesher:**
    ```
    cd /Users/sn0w/Documents/dev/snow
    ./mesher/mesher &
    sleep 2
    ```

    **Seed test data (psql):**
    ```
    psql postgres://mesh:mesh@localhost:5432/mesher -c "
    INSERT INTO users (name, email, password_hash)
      VALUES ('testuser', 'test@test.com', crypt('testpass', gen_salt('bf', 12)))
      ON CONFLICT (email) DO NOTHING;
    INSERT INTO orgs (name, owner_id)
      SELECT 'testorg', id FROM users WHERE email = 'test@test.com'
      ON CONFLICT DO NOTHING;
    INSERT INTO projects (name, org_id)
      SELECT 'testproject', o.id FROM orgs o WHERE o.name = 'testorg'
      ON CONFLICT DO NOTHING;
    INSERT INTO api_keys (key_hash, project_id, name)
      SELECT 'testkey123', p.id, 'testkey' FROM projects p WHERE p.name = 'testproject'
      ON CONFLICT DO NOTHING;
    SELECT id FROM projects WHERE name = 'testproject';"
    ```
    Note the returned PROJECT_ID for use in subsequent curl commands.

    **Test event ingestion (EventProcessor + RateLimiter service call path):**
    ```
    curl -s -o /dev/null -w "event_ingest:%{http_code}\n" \
      -X POST http://localhost:8080/api/v1/events \
      -H "X-Api-Key: testkey123" \
      -H "Content-Type: application/json" \
      -d '{"message":"test error","level":"error","fingerprint":"abc123"}'
    ```
    Expected: 200 or 202 (not 500, not crash). Verify Mesher is still alive after this call.

    **Test search domain:**
    ```
    curl -s -w "search_issues:%{http_code}\n" -o /dev/null \
      http://localhost:8080/api/v1/projects/PROJECT_ID/issues
    ```
    Expected: 200 with JSON array.

    **Test dashboard domain:**
    ```
    curl -s -w "dashboard_volume:%{http_code}\n" -o /dev/null \
      "http://localhost:8080/api/v1/projects/PROJECT_ID/dashboard/volume"
    curl -s -w "dashboard_health:%{http_code}\n" -o /dev/null \
      "http://localhost:8080/api/v1/projects/PROJECT_ID/dashboard/health"
    ```
    Expected: 200 each.

    **Test alerts domain:**
    ```
    curl -s -w "alert_rules:%{http_code}\n" -o /dev/null \
      http://localhost:8080/api/v1/projects/PROJECT_ID/alert-rules
    curl -s -w "alerts:%{http_code}\n" -o /dev/null \
      http://localhost:8080/api/v1/projects/PROJECT_ID/alerts
    ```
    Expected: 200 each.

    **Test settings/retention domain:**
    ```
    curl -s -w "settings:%{http_code}\n" -o /dev/null \
      http://localhost:8080/api/v1/projects/PROJECT_ID/settings
    curl -s -w "storage:%{http_code}\n" -o /dev/null \
      http://localhost:8080/api/v1/projects/PROJECT_ID/storage
    ```
    Expected: 200 each.

    **Test WebSocket upgrade:**
    ```
    curl -s -o /dev/null -w "ws_upgrade:%{http_code}\n" \
      --http1.1 \
      -H "Upgrade: websocket" \
      -H "Connection: Upgrade" \
      -H "Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==" \
      -H "Sec-WebSocket-Version: 13" \
      http://localhost:8081/
    ```
    Expected: 101 (Switching Protocols). A 4xx is acceptable if the WS path requires auth.

    **Confirm no SIGSEGV after all requests:**
    ```
    kill -0 %1 2>/dev/null && echo "PROCESS_ALIVE" || echo "PROCESS_CRASHED"
    ```
    Expected: PROCESS_ALIVE

    **Update SERVICE_CALL_SEGFAULT.md — add this section at the end:**
    ```markdown
    ## Live Verification (Phase 114)

    Rebuilt Mesher from clean with MirType::Tuple fix (types.rs: ptr, not by-value struct). Tested against PostgreSQL.
    - First authenticated POST /api/v1/events: [HTTP status here] — no SIGSEGV
    - Mesher process alive after all endpoint tests: CONFIRMED
    - Status: RESOLVED
    ```
  </action>
  <verify>
    - All HTTP endpoints return 2xx (not 5xx)
    - WebSocket port responds to upgrade (101 or connection accepted)
    - `kill -0 %1` returns 0 (process alive) after all requests
    - SERVICE_CALL_SEGFAULT.md updated with live verification section
  </verify>
  <done>
    All tested HTTP endpoints return 2xx. WebSocket upgrade accepted. Mesher process alive after all requests (no SIGSEGV). SERVICE_CALL_SEGFAULT.md documents resolution. Type "approved" with step results, or describe failures.
  </done>
</task>

</tasks>

<verification>
1. POST /api/v1/events with valid API key returns 2xx and process stays alive — VER-02 (event ingestion path) + SIGSEGV resolved
2. GET endpoints for search, dashboard, alerts, settings, storage all return 200 — VER-02 (all HTTP domains)
3. WebSocket port :8081 returns 101 or responds to upgrade attempt — VER-03
4. SERVICE_CALL_SEGFAULT.md updated with live verification result
</verification>

<success_criteria>
- All tested HTTP endpoints return 2xx (not 5xx, not crash)
- WebSocket endpoint responds to upgrade request
- Mesher process remains alive (no SIGSEGV) after all test requests
- SERVICE_CALL_SEGFAULT.md documents the live verification result
- VER-01, VER-02, VER-03 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/114-compile-run-and-end-to-end-verification/114-02-SUMMARY.md` using the standard summary template.
</output>
