# Required startup contract for the staged reference-backend runtime
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/reference_backend
PORT=18080
JOB_POLL_MS=500

# Optional override for deploy-smoke.sh when probing an already-running instance:
# BASE_URL=http://127.0.0.1:18080
